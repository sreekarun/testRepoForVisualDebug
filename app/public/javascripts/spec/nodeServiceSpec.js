//Node Test Spec File
describe("Node Utilities", function() {

  it("Number of comment nodes  should be 2", function() {
    expect(commentNodes.length).toBe(2);
  });

  it("Number of div nodes  should be 2", function() {
    expect(divNodes.length).toBe(2);
  });



});